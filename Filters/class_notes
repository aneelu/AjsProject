    - used to format the data accroding to application requirement.

    - we have two types of filters.

        1.Predefined Filters
        2.Custom Filters

Predefined Filters.
    The Filters Given by the framework called as Predefined Filter.

1.uppercase.

    - used to convert the lower case characters to the uppercase.

Ex.

    $scope.var_one = 'hello';
    {{var_one}}  // hello
    {{var_one | uppercase }} //HELLO



2.lowercase.

    - used to convert the upper case characters to lowercase.

Ex.

    $scope.var_two = 'HELLO';
    {{var_two}} //HELLO
    {{var_two | lowercase }} //hello


3.currency.
    - used to append the "$" symbol to the numerical values.

Ex.

    $scope.var_three = 100;

    {{var_three}} //100
    {{var_three | currency }} //$100.00
    {{var_three | currency:'$':4}} //$100.0000


4. date.
   - used to format the date accroding application requirement.

Ex.

    $scope.var_four = new Date();

    {{var_four}}
    {{var_four | date:'medium' }}
    {{var_four | date:'high' }}
    {{var_four | date:'short' }}
    {{var_four | date:'dd-MM-yy'}}
    {{var_four | date:'dd/MM/yy'}}
    {{var_four | date:'MM-dd-yyyy'}}
            ---
            ---
            ---

5.orderBy.
    - used to arrange the data either assending order / decending order.

Ex.

     $scope.var_five = [
                        {id:1,name:'Hello_1',age:20},
                        {id:5,name:'Hello_5',age:28},
                        {id:2,name:'Hello_2',age:22},
                        {id:4,name:'Hello_4',age:26},
                        {id:3,name:'Hello_3',age:24}
                       ];


     <tr ng-repeat = "x in var_five">
           <td>{{x.id}}</td>
           <td>{{x.name}}</td>              1   5   2   4   3
           <td>{{x.age}}</td>
     </tr>


     <tr ng-repeat = "x in var_five | orderBy:'id'">
                <td>{{x.id}}</td>
                <td>{{x.name}}</td>          1   2   3   4   5
                <td>{{x.age}}</td>
     </tr>


     <tr ng-repeat = "x in var_five | orderBy:'-id'">
                <td>{{x.id}}</td>
                <td>{{x.name}}</td>      5   4   3   2   1
                <td>{{x.age}}</td>
     </tr>


6.limitTo.
    - used to print the required number of rows from table.


    <tr ng-repeat = "x in var_five | limitTo:3">
               <td>{{x.id}}</td>
               <td>{{x.name}}</td>              1   5   2
               <td>{{x.age}}</td>
    </tr>


    <tr ng-repeat = "x in var_five | orderBy:'id' | limitTo:3">
               <td>{{x.id}}</td>
               <td>{{x.name}}</td>              1   2  3
               <td>{{x.age}}</td>
    </tr>

7.filter
    - used to search the data based on user input.


    <input type="text" ng-model="search">
    <tr ng-repeat = "x in var_five | filter:search">
               <td>{{x.id}}</td>
               <td>{{x.name}}</td>
               <td>{{x.age}}</td>
    </tr>


Custom Filters.
    Creating our own filter based on application requirement called as Custom Filter.

{{'hello' | reverse}}

app.filter('reverse',reverse);
function reverse(){
    return function(arg1){
        var data="";
        for(var i=0;i<arg1.length;i++){
           data = arg1[i]+data;
        }
        return data;
    }
}



<h1>{{100 | my_currency:'₹':true}}</h1>
<h1>{{100 | my_currency:'£':false}}</h1>


app.filter("my_currency" ,my_currency);
function my_currency(){
    return function(amount,symbol,position){
        if(position){
            return symbol+amount;
        }else{
            return amount+symbol;
        }
    }
}






